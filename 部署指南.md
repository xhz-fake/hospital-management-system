# 部署指南

本指南覆盖从环境准备、数据库初始化、后端、Web 管理端、小程序的完整部署流程。若只需快速体验，请先阅读根目录的《快速开始.md》。

## 1. 项目结构与命名

```
hospital-management-system/
├── HospitalManagementSystem_backend/  # SpringBoot3 后端 API
├── hms_miniprogram/                   # 微信小程序（患者端）
├── hms_web_admin/                         # Vue3 Web 管理端（医生端）
└── hms_database/                      # 数据库脚本（init.sql 等）
```

- 文档中提到的 API 前缀为 `/api`，小程序使用 `hms_miniprogram/app.js` 中的 `baseUrl` 进行统一拼接。

## 2. 环境准备

- JDK 17+
- Maven 3.6+
- Node.js 16+
- MySQL 8.0+
- 微信开发者工具

## 3. 数据库初始化

```powershell
mysql -u root -p
CREATE DATABASE IF NOT EXISTS hospital_management CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE hospital_management;
-- 退出后执行脚本
mysql -u root -p hospital_management < hms_database\init.sql
```

## 4. 启动后端

```powershell
cd HospitalManagementSystem_backend
mvn spring-boot:run
```

默认地址：`http://localhost:8080`

## 5. 启动 Web 管理端

```powershell
cd D:\ProgramFiles\CodeProjects\hospital-management-system\hms_web_admin
npm install
npm run dev
```

默认地址：`http://localhost:5173`

## 6. 小程序部署（患者端）

1. 打开微信开发者工具，导入 `hms_miniprogram` 目录
2. 在 `hms_miniprogram/app.js` 中根据实际情况配置 `globalData.baseUrl`，形如：`http://<后端IP>:8080/api`
3. 编译运行，确认“科室选择 -> 医生列表 -> 预约”流程正常

### 6.1 常见问题

- 小程序无法请求：请将 `localhost` 改为本机 IP，或确保手机与后端处于同一网络
- 预约时间解析错误：请参考后端 `Appointment` 的时间格式要求（已支持 `yyyy-MM-dd HH:mm:ss`）

## 7. 其他

- IDE 控制台中文乱码、Maven 下载慢等问题，可参考项目内“维修指南”目录或常见问题汇总

> 本文档用于与《快速开始.md》配套，确保目录命名与引用一致，便于部署与排障。
# 医院管理系统 - 本地部署与调试完整指南

## 📋 目录
1. [环境准备](#环境准备)
2. [数据库部署](#数据库部署)
3. [后端服务部署](#后端服务部署)
4. [Web管理端部署](#web管理端部署)
5. [微信小程序部署](#微信小程序部署)
6. [完整启动流程](#完整启动流程)
7. [调试方法](#调试方法)
8. [常见问题](#常见问题)

---

## 一、环境准备

### 1.1 必需软件清单

在开始之前，请确保你的电脑已安装以下软件：

| 软件 | 版本要求 | 下载地址/检查方法 |
|------|---------|-----------------|
| **JDK** | 17 或更高 | 检查：`java -version`<br>下载：Oracle JDK 或 OpenJDK |
| **Node.js** | 16 或更高 | 检查：`node -v`<br>下载：https://nodejs.org/ |
| **MySQL** | 8.0 或更高 | 检查：`mysql --version`<br>下载：https://dev.mysql.com/downloads/ |
| **Maven** | 3.6 或更高 | 检查：`mvn -version`<br>下载：https://maven.apache.org/ |
| **微信开发者工具** | 最新版 | 下载：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html |

### 1.2 环境检查步骤

**打开 PowerShell 或 CMD，执行以下命令检查环境：**

```powershell
# 检查 Java
java -version
# 应该显示：java version "17.x.x" 或更高

# 检查 Node.js 和 npm
node -v
npm -v
# 应该显示：v16.x.x 或更高

# 检查 MySQL
mysql --version
# 应该显示：mysql Ver 8.0.x 或更高

# 检查 Maven
mvn -version
# 应该显示：Apache Maven 3.6.x 或更高
```

**如果没有安装某个软件，请先安装后再继续。**

---

## 二、数据库部署

### 2.1 创建数据库

1. **启动 MySQL 服务**
   - Windows：在服务管理器中找到 MySQL 服务并启动
   - 或使用命令：`net start MySQL`（以管理员身份运行）

2. **登录 MySQL**
   ```powershell
   mysql -u root -p
   ```
   输入你的 MySQL root 密码

3. **创建数据库**
   在 MySQL 命令行中执行：
   ```sql
   CREATE DATABASE IF NOT EXISTS hospital_management 
   CHARACTER SET utf8mb4 
   COLLATE utf8mb4_unicode_ci;
   
   USE hospital_management;
   
   SHOW DATABASES;  -- 验证数据库是否创建成功
   ```

### 2.2 执行初始化脚本

**方法一：使用命令行（推荐）**

在 PowerShell 中执行（确保已登录 MySQL 或使用完整路径）：
```powershell
cd D:\ProgramFiles\CodeProjects\hospital-management-system\hms_database
mysql -u root -p hospital_management < init.sql
```

**方法二：使用 MySQL 客户端工具（如 Navicat、MySQL Workbench）**

1. 打开 MySQL 客户端工具
2. 连接到 MySQL 服务器
3. 选择 `hospital_management` 数据库
4. 打开并执行 `D:\ProgramFiles\CodeProjects\hospital-management-system\database\init.sql` 文件

### 2.3 验证数据库

登录 MySQL 后执行：
```sql
USE hospital_management;
SHOW TABLES;
```

应该看到以下表：
- `patients`（患者表）
- `doctors`（医生表）
- `departments`（科室表）
- `appointments`（预约表）
- `schedules`（排班表）
- `medical_records`（病历表）
- `notifications`（通知消息表）

执行以下命令查看是否有测试数据：
```sql
SELECT COUNT(*) FROM doctors;  -- 应该返回 10（有10个测试医生）
SELECT COUNT(*) FROM patients; -- 查看患者数量
SELECT COUNT(*) FROM notifications; -- 查看通知消息数量
```

---

## 三、后端服务部署

### 3.1 配置数据库连接

**打开文件：** `D:\ProgramFiles\CodeProjects\hospital-management-system\HospitalManagementSystem_backend\src\main\resources\application.yml`

**修改数据库配置：**(建议为用户名和密码加上双引号；IP 地址使用 `127.0.0.1`)

```yaml
spring:        
datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/hospital_management?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: "root"     # 改为你的 MySQL 用户名
    password: "<你的MySQL密码>"    # 改为你的 MySQL 密码
```

⚠️ **重要：** 请根据你的实际 MySQL 配置修改 `username` 和 `password`

### 3.2 安装 Maven 依赖（首次运行）

```powershell
cd D:\ProgramFiles\CodeProjects\hospital-management-system\HospitalManagementSystem_backend
mvn clean install
```

**这个过程可能需要几分钟，Maven 会自动下载所需的依赖包。**

如果下载很慢，可以配置国内镜像源（在 Maven 安装目录的 `conf/settings.xml` 中添加阿里云镜像）。

### 3.3 启动后端服务

**方法一：使用 Maven 命令启动（开发推荐）**
```powershell
cd D:\ProgramFiles\CodeProjects\hospital-management-system\HospitalManagementSystem_backend
mvn spring-boot:run
```

**方法二：打包后运行（生产环境）**
```powershell
# 打包
mvn clean package

# 运行（使用通配符以适配实际版本号/Artifact 名称）
java -jar target\*.jar
```

### 3.4 验证后端服务

启动成功后，你会看到类似以下日志：
```
Started HospitalManagementApplication in X.XXX seconds
```

**测试后端 API：**

1. **打开浏览器尝试访问：** http://localhost:8080/api/doctors/list
   - 应该返回 JSON 格式的医生列表数据

2. **或者使用 PowerShell 测试：**
```powershell
curl http://localhost:8080/api/doctors/list
```

如果返回数据，说明后端启动成功！

**后端服务地址：** http://localhost:8080/api

---

### 3.5 使用 IntelliJ IDEA 构建与调试（推荐）

#### 3.5.1 安装与准备
- 安装 IntelliJ IDEA（社区版/旗舰版均可）
- 确保本机已安装 JDK 17+、Maven 3.6+（见本指南前文“环境准备”）
- 建议安装插件：Lombok（Preferences/Settings → Plugins → 搜索 Lombok → Install）

#### 3.5.2 导入项目（Maven 项目）
1. 打开 IDEA → File → Open
2. 选择目录：`D:\ProgramFiles\CodeProjects\hospital-management-system\HospitalManagementSystem_backend`
3. 看到 `pom.xml` 后，选择“Open as Project”
4. 首次导入时，IDEA 会自动识别为 Maven 项目并开始索引与下载依赖

如果未自动识别：
- 右侧 Maven 工具窗口（View → Tool Windows → Maven）中点击“Reload All Maven Projects”

#### 3.5.3 配置 JDK 与 Maven
1. File → Project Structure → Project
   - Project SDK：选择 `17`（或已安装的 17+ JDK）
   - Project language level：`SDK default`（或 `17`）
2. File → Settings（Preferences on macOS）→ Build, Execution, Deployment → Build Tools → Maven
   - Maven home path：可用自带（Bundled）或本机 Maven 安装目录
   - User settings file：如有私有镜像，可指向自定义 `settings.xml`

#### 3.5.4 使能 Lombok 与注解处理
1. 安装 Lombok 插件（见 3.5.1）
2. File → Settings → Build, Execution, Deployment → Compiler → Annotation Processors
   - 勾选 “Enable annotation processing”

#### 3.5.5 配置数据库连接（仅确认）
检查 `HospitalManagementSystem_backend/src/main/resources/application.yml` 中：
```
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/hospital_management?...
    username: "root"
    password: "<你的MySQL密码>"
```
若你使用了专用账号，改为对应用户名与密码。

#### 3.5.6 构建与运行（两种方式）(目前)
- 方式一：使用 Maven 生命周期
  1. 右侧 Maven 工具窗口 → Lifecycle → 双击 `clean`、`install`
  2. 构建成功后，在 Maven 工具窗口或 Run 窗口查看日志

- 方式二：直接运行 Spring Boot 主类（推荐调试）
  1. 在 `src/main/java` 下找到启动类（通常为 `...Application`，例如 `HospitalManagementApplication`）
  2. 右键该类 → Run 'HospitalManagementApplication'
  3. 首次运行会生成一个 Run Configuration，后续可直接使用

#### 3.5.7 调试（Debug）
1. 在需要调试的 Controller/Service 代码行设置断点
2. 右键主类 → Debug 'HospitalManagementApplication'
3. 通过浏览器或前端发起请求，IDEA 会在断点处暂停，支持查看变量、单步调试

#### 3.5.8 常见问题及注意事项（IDEA）

- 最好只用一种方式启动项目进程：

  - 要么 IDEA 的  *Run/Debug*

  - 要么 PowerShell 的 *mvn spring-boot:run* 或 java -jar target\xxx.jar

- 依赖下载缓慢或失败：

  - 配置 Maven 阿里云镜像（`settings.xml`）
  - 点击 Maven 工具窗口的"Reload All Maven Projects"

- Lombok 相关报错（如 Getter/Setter 未识别）：
  - 确认已安装 Lombok 插件并启用 Annotation Processing

- `java: invalid target release`：
  - Project SDK 未设置为 17+；检查 Project Structure 与 Maven `maven-compiler-plugin` 配置

- **IDE控制台中文乱码问题**（重要）：
  
  如果启动后浏览器显示正常，但IDEA控制台中的日志或数据信息显示为乱码，请按以下步骤解决：
  
  1. **检查项目编码设置**：
     - File → Settings → Editor → File Encodings
     - 确保以下选项都设置为 `UTF-8`：
       - Global Encoding: UTF-8
       - Project Encoding: UTF-8
       - Default encoding for properties files: UTF-8
       - 勾选 "Transparent native-to-ascii conversion"
  
  2. **检查IDEA运行配置编码**：
     - Run → Edit Configurations
     - 找到你的运行配置（如 HospitalManagementApplication）
     - 在 VM options 中添加：`-Dfile.encoding=UTF-8`
     - 在 Environment variables 中添加：`JAVA_TOOL_OPTIONS=-Dfile.encoding=UTF-8`
  
  3. **检查控制台编码**：
     - File → Settings → Editor → General → Console
     - 确保 "Default Encoding" 设置为 `UTF-8`
  
  4. **项目已配置字符编码**：
     - 项目已添加 `CharsetConfig.java` 配置类，确保HTTP响应使用UTF-8
     - `application.yml` 中已配置 `server.servlet.encoding` 和 `spring.http.encoding`
     - 数据库连接URL已包含 `useUnicode=true&characterEncoding=utf8`
  
  5. **如果仍有问题**：
     - 重启IDEA
     - 清理并重新构建项目：Build → Rebuild Project
     - 检查系统环境变量：确保没有设置 `JAVA_TOOL_OPTIONS` 为其他编码

- 端口占用（8080）：
  - 修改 `application.yml` 的 `server.port`
  - 同步修改前端 `vite.config.js` 代理与小程序 `app.js` 中的 `baseUrl`
  - 若有报错: 

  ```
  Web server failed to start. Port 8080 was already in use.
  ```

  表示有进程占用了8080 , 但一般是我们刚才构建项目成功过一次了, 因为Spring Boot 是 Web服务器, 启动后会持续监听端口，直到你手动停止。同一端口同一时刻只能有一个实例占用。而小脚本项目执行完就退出

  - 如果想要结束进程重启:

    - 如果你是使用PowerShell启动: 

      - 在cmd命令行输入以下代码杀死进程

        - ```
              netstat -ano | findstr :8080
              taskkill /PID <PID> /F
          ```

          把<PID>换成第一行里监听8080的数字

    - 如果你是在IDEA里通过RUN来执行启动类, 然后彻底结束进程

### 3.6 启动失败（H2 Driver）快速修复

如果启动时报错 “Cannot load driver class: org.h2.Driver”，说明运行期没有正确读取到 MySQL 数据源配置。可用如下方式临时修复并验证：

PowerShell（使用 SPRING_APPLICATION_JSON 注入配置）
```powershell
cd "D:\ProgramFiles\CodeProjects\hospital-management-system\HospitalManagementSystem_backend"
$env:SPRING_APPLICATION_JSON='{"spring":{"datasource":{"url":"jdbc:mysql://localhost:3306/hospital_management?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true","driver-class-name":"com.mysql.cj.jdbc.Driver","username":"root","password":"<你的密码>"}}}'
mvn -q -DskipTests spring-boot:run
```

说明：
- 以上命令只影响当前终端会话；用于快速确认问题在于数据源绑定。
- 正常情况下，`HospitalManagementSystem_backend/src/main/resources/application.yml` 中的数据源配置应被读取，无需命令行参数。若使用 Druid 导致绑定异常，可先按最小化配置仅保留 `spring.datasource.url/username/password/driver-class-name`（使用默认 Hikari），确认成功后再恢复连接池配置。

---

## 四、Web管理端部署

### 📚 前置知识：前后端通信原理（初学者必读）

在开始部署之前，让我们先理解一下前后端是如何通信的：

#### 4.0.1 什么是前后端分离？

- **前端（Web管理端）**：运行在浏览器中，负责显示界面和用户交互
- **后端（Spring Boot）**：运行在服务器上，负责处理业务逻辑和数据库操作
- **通信方式**：前端通过 HTTP 请求调用后端 API，后端返回 JSON 数据

#### 4.0.2 前后端通信流程

```
用户操作（点击按钮）
    ↓
前端发送 HTTP 请求（如：GET /api/doctors/list）
    ↓
Vite 开发服务器接收请求（端口 5173）
    ↓
根据 vite.config.js 的 proxy 配置，转发到后端
    ↓
后端 Spring Boot 处理请求（端口 8080）
    ↓
后端查询数据库并返回 JSON 数据
    ↓
前端接收数据并更新页面显示
```

#### 4.0.3 关键配置文件说明

1. **`vite.config.js`** - Vite 开发服务器配置
   - `port: 5173`：前端开发服务器运行在 5173 端口
   - `proxy`：代理配置，将 `/api` 开头的请求转发到后端
   - `target: 'http://localhost:8080'`：后端服务地址

2. **`src/api/index.js`** - API 请求配置
   - `baseURL: '/api'`：所有 API 请求的前缀
   - 当代码中调用 `api.get('/doctors/list')` 时，实际请求的是 `http://localhost:5173/api/doctors/list`
   - Vite 代理会将这个请求转发到 `http://localhost:8080/api/doctors/list`

#### 4.0.4 为什么需要代理？

- **开发环境**：前端（5173）和后端（8080）运行在不同端口
- **跨域问题**：浏览器安全策略不允许直接跨端口请求
- **解决方案**：使用 Vite 的 proxy 功能，让前端请求看起来像是同源的

---

### 4.1 环境检查

在开始之前，确保：

1. ✅ **后端服务已启动**（见第三章）
   - 后端必须运行在 `http://localhost:8080`
   - 可以通过浏览器访问 `http://localhost:8080/api/doctors/list` 验证

2. ✅ **Node.js 已安装**
   ```powershell
   node -v
   # 应该显示：v16.x.x 或更高
   
   npm -v
   # 应该显示：8.x.x 或更高
   ```

3. ✅ **网络连接正常**（npm 需要下载依赖包）

---

### 4.2 安装依赖

#### 4.2.1 什么是 npm 和依赖？

- **npm**：Node.js 的包管理器，用于安装和管理 JavaScript 库
- **依赖**：项目需要的第三方库（如 Vue、Element Plus、Axios 等）
- **`package.json`**：记录项目需要的所有依赖

#### 4.2.2 安装步骤

**步骤 1：打开 PowerShell 或 CMD**

**步骤 2：进入项目目录**

```powershell
cd D:\ProgramFiles\CodeProjects\hospital-management-system\hms_web_admin
```

**步骤 3：安装依赖**
```powershell
npm install
```

**这个过程会：**
- 读取 `package.json` 文件
- 下载所有列出的依赖包到 `node_modules` 文件夹
- 可能需要 2-5 分钟，取决于网络速度

**预期输出：**
```
added 200+ packages, and audited 250+ packages in 30s
```

#### 4.2.3 如果下载很慢怎么办？

**配置国内镜像源（推荐）：**

```powershell
# 设置 npm 使用淘宝镜像（国内访问更快）
npm config set registry https://registry.npmmirror.com

# 验证是否设置成功
npm config get registry
# 应该显示：https://registry.npmmirror.com

# 然后重新执行安装
npm install
```

**临时使用镜像（仅本次安装）：**
```powershell
npm install --registry=https://registry.npmmirror.com
```

#### 4.2.4 安装失败怎么办？

**问题 1：网络超时**
```powershell
# 清除 npm 缓存
npm cache clean --force

# 重新安装
npm install
```

**问题 2：权限错误**
- Windows：以管理员身份运行 PowerShell
- 或使用：`npm install --legacy-peer-deps`

**问题 3：node_modules 损坏**
```powershell
# 删除 node_modules 文件夹和 package-lock.json
Remove-Item -Recurse -Force node_modules
Remove-Item package-lock.json

# 重新安装
npm install
```

---

### 4.3 检查配置

#### 4.3.1 查看 vite.config.js

**文件位置：** `D:\ProgramFiles\CodeProjects\hospital-management-system\web-admin\vite.config.js`

**打开文件，检查以下配置：**

```javascript
export default defineConfig({
  server: {
    port: 5173,  // 前端开发服务器端口
    proxy: {
      '/api': {
        target: 'http://localhost:8080',  // ⚠️ 后端服务地址
        changeOrigin: true
      }
    }
  }
})
```

**配置说明：**
- `port: 5173`：前端运行在 5173 端口
- `target: 'http://localhost:8080'`：后端服务地址
- `changeOrigin: true`：改变请求的 origin，解决跨域问题

#### 4.3.2 什么时候需要修改配置？

**情况 1：后端端口不是 8080**
```javascript
target: 'http://localhost:8081',  // 改为你的后端端口
```

**情况 2：后端运行在其他机器**

```javascript
target: 'http://192.168.1.100:8080',  // 改为后端服务器的 IP
```

**情况 3：前端端口被占用**

```javascript
port: 5174,  // 改为其他可用端口
```

---

### 4.4 启动开发服务器

#### 4.4.1 启动步骤

**步骤 1：确保在正确的目录**

```powershell
cd D:\ProgramFiles\CodeProjects\hospital-management-system\hms_web_admin
```

**步骤 2：启动开发服务器**

```powershell
npm run dev
```

**步骤 3：等待启动完成**

你会看到类似以下输出：
```
  VITE v5.0.0  ready in 1234 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: http://192.168.1.100:5173/
  ➜  press h + enter to show help
```

**关键信息：**
- ✅ `ready in xxx ms`：启动成功
- ✅ `Local: http://localhost:5173/`：本地访问地址
- ✅ `Network: http://192.168.1.100:5173/`：局域网访问地址（其他设备可访问）

#### 4.4.2 启动失败怎么办？

**问题 1：端口 5173 被占用**
```
Error: listen EADDRINUSE: address already in use :::5173
```

**解决方法：**
- 方法 A：关闭占用 5173 端口的程序
  ```powershell
  # 查找占用端口的进程
  netstat -ano | findstr :5173
  # 结束进程（替换 <PID> 为实际进程ID）
  taskkill /PID <PID> /F
  ```

- 方法 B：修改 vite.config.js 使用其他端口
  ```javascript
  port: 5174,  // 改为其他端口
  ```

**问题 2：找不到命令 `npm`**
```
npm : 无法将"npm"项识别为 cmdlet、函数、脚本文件或可运行程序的名称
```

**解决方法：**
- 检查 Node.js 是否安装：`node -v`
- 如果未安装，请先安装 Node.js：https://nodejs.org/

**问题 3：模块未找到**
```
Error: Cannot find module 'xxx'
```

**解决方法：**
```powershell
# 重新安装依赖
npm install
```

---

### 4.5 访问Web管理端

#### 4.5.1 打开浏览器

**在浏览器地址栏输入：**
```
http://localhost:5173
```

**或者点击终端中显示的链接：**
```
➜  Local:   http://localhost:5173/
```

#### 4.5.2 预期看到的内容

1. **登录页面**
   - 标题：医院管理系统 - 医生端
   - 用户名输入框
   - 密码输入框
   - 登录按钮

2. **如果看到空白页面或错误**
   - 打开浏览器开发者工具（按 F12）
   - 查看 Console 标签页的错误信息
   - 查看 Network 标签页的请求状态

#### 4.5.3 测试登录

**使用测试账号登录：**

| 用户名 | 密码 | 说明 |
|--------|------|------|
| `doctor1` | `123456` | 医生1 |
| `doctor2` | `123456` | 医生2 |
| ... | ... | ... |
| `doctor10` | `123456` | 医生10 |

**登录步骤：**
1. 输入用户名（如：`doctor1`）
2. 输入密码（`123456`）
3. 点击"登录"按钮
4. 如果成功，会跳转到工作台页面

#### 4.5.4 登录成功后的页面

登录成功后，你应该能看到：
- **工作台**：显示数据概览和今日预约
- **左侧菜单**：预约管理、患者管理、排班管理、病历管理
- **右上角**：显示当前登录医生的信息

---

### 4.6 验证前后端通信

#### 4.6.1 使用浏览器开发者工具

**步骤 1：打开开发者工具**
- 按 `F12` 键
- 或右键页面 → 选择"检查"

**步骤 2：查看 Network 标签页**
- 点击 Network（网络）标签
- 刷新页面（F5）
- 查看是否有 `/api/` 开头的请求

**步骤 3：检查请求状态**
- ✅ **绿色（200）**：请求成功
- ❌ **红色（404/500）**：请求失败
- ⚠️ **黄色（pending）**：请求中

**步骤 4：查看请求详情**
- 点击任意一个 `/api/` 请求
- 查看：
  - **Request URL**：请求地址（应该是 `http://localhost:5173/api/...`）
  - **Status Code**：状态码（200 表示成功）
  - **Response**：响应数据（JSON 格式）

#### 4.6.2 测试 API 请求

**在 Console 标签页中测试：**

```javascript
// 测试获取医生列表
fetch('/api/doctors/list')
  .then(res => res.json())
  .then(data => console.log('医生列表：', data))
  .catch(err => console.error('错误：', err))
```

**预期输出：**
```javascript
医生列表： {
  code: 200,
  message: "操作成功",
  data: {
    records: [...],  // 医生列表数据
    total: 10
  }
}
```

---

### 4.7 常见问题排查

#### 4.7.1 页面空白或加载失败

**可能原因 1：后端服务未启动**
- **检查方法**：访问 `http://localhost:8080/api/doctors/list`
- **解决方法**：启动后端服务（见第三章）

**可能原因 2：代理配置错误**
- **检查方法**：查看 `vite.config.js` 中的 `target` 配置
- **解决方法**：确保 `target` 指向正确的后端地址

**可能原因 3：端口冲突**
- **检查方法**：查看终端错误信息
- **解决方法**：修改 `vite.config.js` 中的 `port` 配置

#### 4.7.2 登录失败

**可能原因 1：后端未启动**
- **现象**：点击登录后，页面提示"网络错误"
- **解决方法**：启动后端服务

**可能原因 2：用户名或密码错误**
- **现象**：提示"用户名或密码错误"
- **解决方法**：使用正确的测试账号（doctor1-doctor10，密码：123456）

**可能原因 3：数据库未初始化**
- **现象**：后端返回 500 错误
- **解决方法**：执行数据库初始化脚本（见第二章）

#### 4.7.3 API 请求返回 404

**可能原因 1：后端路径不匹配**
- **检查**：后端 `application.yml` 中的 `context-path` 配置
- **应该为**：`context-path: /api`
- **前端请求**：`/api/doctors/list`
- **实际后端路径**：`/api/doctors/list`（正确）

**可能原因 2：后端 Controller 路径错误**
- **检查**：后端 Controller 的 `@RequestMapping` 注解
- **应该为**：`@RequestMapping("/doctors")`

#### 4.7.4 页面显示乱码

**可能原因：文件编码问题**
- **解决方法**：
  1. 确保所有 `.vue` 和 `.js` 文件使用 UTF-8 编码
  2. 检查 `index.html` 中的 `<meta charset="UTF-8">`

---

### 4.8 开发调试技巧

#### 4.8.1 热更新（Hot Module Replacement）

**什么是热更新？**
- 修改代码后，页面自动刷新，无需手动刷新浏览器
- Vite 默认启用，非常方便开发

**如何使用？**
1. 修改任意 `.vue` 或 `.js` 文件
2. 保存文件（Ctrl+S）
3. 浏览器自动刷新，显示最新内容

#### 4.8.2 使用浏览器开发者工具调试

**Console 标签页：**

- 查看 `console.log()` 输出
- 执行 JavaScript 代码
- 查看错误信息

**Network 标签页：**
- 查看所有 HTTP 请求
- 检查请求和响应数据
- 分析请求耗时

**Elements 标签页：**
- 查看页面 HTML 结构
- 实时修改样式
- 查看元素绑定的数据

**Application 标签页：**
- 查看 LocalStorage、SessionStorage
- 查看 Cookie
- 查看缓存数据

#### 4.8.3 使用 Vue DevTools（可选）

**安装 Vue DevTools 浏览器扩展：**
1. Chrome：在 Chrome 网上应用店搜索 "Vue.js devtools"
2. Firefox：在 Firefox 附加组件中搜索 "Vue.js devtools"

**功能：**
- 查看组件树
- 查看组件数据（data、props、computed）
- 查看 Pinia 状态
- 查看 Vue Router 路由

---

### 4.9 停止开发服务器

**方法 1：在终端中按快捷键**
- 按 `Ctrl + C`
- 确认停止（输入 `Y` 或直接回车）

**方法 2：关闭终端窗口**
- 直接关闭 PowerShell 或 CMD 窗口

**注意：**
- 停止后，前端页面将无法访问
- 需要重新运行 `npm run dev` 才能再次访问

---

### 4.10 生产环境构建（可选）

如果你需要将前端部署到生产环境：

```powershell
# 构建生产版本
npm run build

# 构建完成后，会在 dist 目录生成静态文件
# 可以将 dist 目录部署到 Nginx、Apache 等 Web 服务器
```

**构建后的文件：**
- `dist/index.html`：入口 HTML 文件
- `dist/assets/`：JavaScript、CSS 等资源文件

---

### 4.11 总结

完成 Web 管理端部署后，你应该能够：

1. ✅ 成功安装所有依赖
2. ✅ 启动开发服务器
3. ✅ 在浏览器中访问登录页面
4. ✅ 使用测试账号成功登录
5. ✅ 查看工作台和各个功能页面
6. ✅ 使用浏览器开发者工具调试

**下一步：**
- 如果一切正常，可以继续部署微信小程序（见第五章）
- 如果遇到问题，参考本章的"常见问题排查"部分

---

## 五、微信小程序部署

### 📚 前置知识：小程序与后端通信原理（初学者必读）

在开始部署之前，让我们先理解一下小程序是如何与后端通信的：

#### 5.0.1 小程序与 Web 端的区别

- **小程序**：运行在微信客户端中，通过微信提供的 API 与服务器通信
- **Web 端**：运行在浏览器中，直接通过 HTTP 请求与服务器通信
- **主要区别**：
  - 小程序有域名白名单限制（正式环境必须配置合法域名）
  - 小程序不能直接使用 `localhost`，需要使用 IP 地址或域名
  - 小程序的网络请求需要通过 `wx.request` API

#### 5.0.2 小程序网络请求流程

```
用户操作（点击按钮）
    ↓
小程序调用 wx.request()
    ↓
发送 HTTP 请求到后端服务器（如：http://192.168.1.100:8080/api/doctors/list）
    ↓
后端 Spring Boot 处理请求（端口 8080）
    ↓
后端查询数据库并返回 JSON 数据
    ↓
小程序接收数据并更新页面显示
```

#### 5.0.3 关键配置文件说明

1. **`app.js`** - 小程序入口文件
   - `globalData.baseUrl`：后端 API 地址
   - `request()` 方法：封装了 `wx.request`，统一处理网络请求

2. **`project.config.json`** - 小程序项目配置
   - `appid`：小程序 AppID
   - `compileType`：编译类型（miniprogram）
   - `setting`：项目设置

3. **`app.json`** - 小程序全局配置
   - `pages`：页面路径列表
   - `window`：窗口表现
   - `tabBar`：底部导航栏

#### 5.0.4 为什么不能使用 localhost？

- **小程序安全限制**：微信小程序为了安全，不允许直接访问 `localhost`
- **解决方案**：
  - **开发环境**：使用本机 IP 地址（如 `192.168.1.100`）
  - **正式环境**：必须配置合法的服务器域名（如 `https://api.example.com`）
  - **开发工具**：勾选"不校验合法域名"选项（仅开发时有效）

---

### 5.1 环境检查

在开始之前，确保：

1. ✅ **后端服务已启动**（见第三章）
   - 后端必须运行在 `http://localhost:8080`
   - 可以通过浏览器访问 `http://localhost:8080/api/doctors/list` 验证

2. ✅ **微信开发者工具已安装**
   - 下载地址：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
   - 安装后使用微信扫码登录

3. ✅ **获取本机 IP 地址**（重要）
   ```powershell
   # 在 PowerShell 中执行
   ipconfig
   # 查找"IPv4 地址"，例如：192.168.1.100
   ```

---

### 5.2 打开微信开发者工具

#### 5.2.1 启动微信开发者工具

1. **启动微信开发者工具**
   - 双击桌面图标
   - 或从开始菜单启动

2. **登录微信账号**
   - 使用微信扫码登录
   - 如果没有微信开发者账号，可以使用测试号

#### 5.2.2 导入项目

1. **点击"+"或"导入项目"按钮**
   - 在微信开发者工具主界面
   - 选择"导入项目"

2. **选择项目目录**
   - 点击"目录"后面的"选择"按钮
   - 选择：`D:\ProgramFiles\CodeProjects\hospital-management-system\hms_miniprogram`
   - ⚠️ **注意**：选择 `hms_miniprogram` 目录，不是项目根目录

3. **填写项目信息**
   - **AppID**：选择"测试号"或"不使用云服务"（开发测试）
   - 如果有正式小程序账号，填入真实的 AppID
   - **项目名称**：医院管理系统（可自定义）

4. **点击"导入"**
   - 等待项目加载完成
   - 如果出现错误，检查目录路径是否正确

---

### 5.3 配置小程序信息

#### 5.3.1 AppID 配置说明

**什么是 AppID？**
- AppID 是小程序的唯一标识
- 正式发布的小程序必须有 AppID
- 开发测试可以使用"测试号"或"不使用云服务"

**如何获取 AppID？**
1. **正式 AppID**（需要微信小程序账号）：
   - 访问：https://mp.weixin.qq.com/
   - 注册小程序账号
   - 在"开发" → "开发管理" → "开发设置"中查看 AppID

2. **测试号**（开发测试）：
   - 在微信开发者工具中选择"测试号"
   - 或选择"不使用云服务"
   - ⚠️ **注意**：测试号只能用于开发测试，不能发布

#### 5.3.2 项目名称配置

- **项目名称**：医院管理系统（可自定义）
- 这个名称只显示在开发者工具中，不影响小程序实际运行

---

### 5.4 配置 API 地址（重要）

#### 5.4.1 获取本机 IP 地址

**步骤 1：打开 PowerShell**

**步骤 2：执行命令**
```powershell
ipconfig
```

**步骤 3：查找 IPv4 地址**
```
无线局域网适配器 WLAN:
   IPv4 地址 . . . . . . . . . . . . : 192.168.1.100
  子网掩码  . . . . . . . . . . . . : 255.255.255.0
```

记录你的 IPv4 地址（例如：`192.168.1.100`）

#### 5.4.2 修改 app.js 配置

**文件位置：** `D:\ProgramFiles\CodeProjects\hospital-management-system\hms_miniprogram\app.js`

**步骤 1：打开文件**
- 在微信开发者工具中，左侧文件树找到 `app.js`
- 双击打开

**步骤 2：修改 baseUrl**
```javascript
App({
  globalData: {
    baseUrl: 'http://192.168.1.100:8080/api',  // ⚠️ 将 192.168.1.100 改为你的本机 IP
    userInfo: null,
    patients: [] // 当前用户绑定的就诊人列表
  },
  // ... 其他代码
})
```

**步骤 3：保存文件**
- 按 `Ctrl + S` 保存
- 微信开发者工具会自动编译

#### 5.4.3 配置说明

⚠️ **重要说明：**

1. **开发环境配置**：
   - 使用本机 IP 地址（如 `http://192.168.1.100:8080/api`）
   - 不要使用 `localhost` 或 `127.0.0.1`（小程序无法访问）

2. **生产环境配置**：
   - 使用服务器域名（如 `https://api.example.com/api`）
   - 必须使用 HTTPS（微信小程序要求）
   - 需要在微信公众平台配置服务器域名白名单

3. **端口号**：
   - 确保端口号与后端服务端口一致（默认 8080）
   - 如果后端使用了其他端口，需要同步修改

4. **API 路径**：
   - 确保路径以 `/api` 结尾
   - 与后端 `application.yml` 中的 `context-path` 配置一致

#### 5.4.4 验证配置

**方法 1：查看网络请求**
1. 在微信开发者工具中，点击"编译"按钮
2. 打开"调试器" → "Network" 标签
3. 在小程序中操作（如查看医生列表）
4. 查看 Network 中的请求地址是否正确

**方法 2：查看控制台**
1. 打开"调试器" → "Console" 标签
2. 查看是否有网络请求错误
3. 如果有错误，检查 `baseUrl` 配置是否正确

---

### 5.5 配置网络请求权限

#### 5.5.1 开发环境配置（不校验合法域名）

**步骤 1：打开项目设置**
1. 在微信开发者工具中，点击右上角"详情"按钮
2. 选择"本地设置"标签

**步骤 2：勾选开发选项**
- ✅ 勾选"不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书"
- ✅ 勾选"不校验安全域名、TLS 版本以及 HTTPS 证书"

⚠️ **重要说明：**
- 这些选项**仅用于开发测试**
- **正式发布时必须取消勾选**
- 正式发布前需要在微信公众平台配置服务器域名白名单

#### 5.5.2 生产环境配置（配置服务器域名）

**步骤 1：登录微信公众平台**
1. 访问：https://mp.weixin.qq.com/
2. 使用小程序管理员账号登录

**步骤 2：配置服务器域名**
1. 进入"开发" → "开发管理" → "开发设置"
2. 找到"服务器域名"配置
3. 点击"修改"按钮
4. 添加后端服务器域名（如 `https://api.example.com`）
5. 保存配置

**步骤 3：配置 request 合法域名**
- 在"服务器域名"中，找到"request 合法域名"
- 添加后端 API 地址（如 `https://api.example.com`）
- ⚠️ **注意**：必须使用 HTTPS，不能使用 HTTP

---

### 5.6 运行小程序

#### 5.6.1 编译小程序

**步骤 1：点击"编译"按钮**
- 在微信开发者工具顶部工具栏
- 点击"编译"按钮（或按 `Ctrl + B`）

**步骤 2：等待编译完成**
- 编译过程中，底部会显示编译进度
- 编译成功后，模拟器会自动刷新

**步骤 3：查看编译结果**
- 如果编译成功，模拟器会显示小程序界面
- 如果编译失败，查看底部"编译"标签的错误信息

#### 5.6.2 测试小程序功能

**测试步骤：**

1. **测试首页**
   - 查看首页是否正常显示
   - 测试搜索功能
   - 测试功能模块点击

2. **测试科室选择**
   - 点击"科室"菜单
   - 查看科室列表是否正常显示
   - 测试科室选择功能

3. **测试医生列表**
   - 选择科室后，查看医生列表
   - 测试医生详情页面
   - 测试预约功能

4. **测试预约功能**
   - 选择医生和时间
   - 创建预约
   - 查看"我的预约"页面

5. **测试就诊人管理**
   - 添加就诊人
   - 编辑就诊人信息
   - 删除就诊人

#### 5.6.3 查看调试信息

**Console 面板：**
1. 点击"调试器" → "Console" 标签
2. 查看 `console.log()` 输出
3. 查看错误信息

**Network 面板：**
1. 点击"调试器" → "Network" 标签
2. 查看网络请求
3. 检查请求是否成功（状态码 200）
4. 查看响应数据

**Storage 面板：**
1. 点击"调试器" → "Storage" 标签
2. 查看本地存储数据
3. 检查就诊人列表等缓存数据

---

### 5.7 真机调试

#### 5.7.1 预览小程序

**步骤 1：点击"预览"按钮**
- 在微信开发者工具顶部工具栏
- 点击"预览"按钮

**步骤 2：扫码预览**
- 会生成一个二维码
- 使用微信扫码（必须是管理员或开发者）
- 在手机上打开小程序

**步骤 3：测试功能**
- 在手机上测试小程序功能
- 检查网络请求是否正常
- 检查界面显示是否正常

#### 5.7.2 真机调试

**步骤 1：开启真机调试**
1. 在微信开发者工具中，点击"预览"按钮
2. 扫码后，在手机上点击"打开调试"
3. 重新扫码进入小程序

**步骤 2：查看调试信息**
- 在微信开发者工具的"调试器"中
- 可以查看手机端的 Console 输出
- 可以查看网络请求
- 可以查看错误信息

**步骤 3：注意事项**
- 确保手机和电脑在同一网络
- 确保后端服务可以被手机访问（使用本机 IP）
- 确保防火墙没有阻止 8080 端口

### 5.8 网络连通方案与常见拓扑

| 场景 | 说明 | 配置要点 |
| --- | --- | --- |
| 同一局域网 Wi-Fi | 手机与电脑连接同一路由器，最常见的调试方式 | 关闭路由器 AP 隔离；电脑开放 8080 端口；`baseUrl` 使用电脑 IPv4 地址 |
| 手机热点 | 电脑连接手机热点（你目前采用的方法） | 适合应急，流量成本高；建议保留为备用方案 |
| 内网穿透 | 通过 `ngrok` / `frp` / `natapp` 等工具将本地 8080 暴露为公网地址 | 生成的 https 域名需要配置到微信「request 合法域名」；`baseUrl` 使用该域名 |
| 云服务器 | 将后端、数据库部署在云端（如华为云、阿里云） | 云主机开放 80/443/8080；结合域名和 SSL；微信后台登记 https 合法域名 |

> ✅ 微信小程序真机在开发阶段允许访问 http 的内网 IP，但前提是开发者工具勾选「不校验合法域名」。正式上线必须改用 https 域名并在微信后台登记。

**推荐流程：**
1. 局域网调试阶段：手机与电脑同网段 → `baseUrl` 写电脑 IP。
2. 如局域网受限，可临时用手机热点或内网穿透。
3. 准备参赛/上线：迁移到云服务器 + https 域名，从根本上解决网络隔离问题。

### 5.9 病历模块全链路说明

病历涉及医生端与患者端双向联动，核心流程如下：

1. **医生端上传附件**
   - 调用 `POST /api/files/upload` 上传图片/文件。
   - 后端写入 `uploads/` 目录并返回完整访问 URL（如 `https://<域名>/api/files/<uuid>.jpg`）。
2. **医生端创建病历**
   - 调用 `POST /api/medical-records/create`，必须包含：`patientId`、`doctorId`、诊断/治疗/处方/病历内容、`fileUrl`。
   - `medical_records` 表会记录关联的医生、患者、预约及附件地址。
3. **患者端（小程序）拉取病历**
   - `GET /api/medical-records/patient/{patientId}`，按时间倒序返回列表。
   - 小程序内根据 `fileUrl` 判断是否为图片，调用 `wx.previewImage` 或提示复制链接。
4. **医生端查看病历**
   - Web 管理端同样请求 `GET /api/medical-records/patient/{patientId}`，在界面中显示详情并允许直接打开附件（浏览器负责展示或下载）。
5. **URL 兜底策略**
   - 后端优先返回绝对 URL；若历史数据存在相对路径，前端会自动拼接 `baseUrl` 或 `/api` 以保证可达。

> 附件当前存储在后端服务器磁盘，若迁移到云对象存储（OBS），只需让上传接口返回 OBS 的外链 URL，其余流程无需改动。

---

### 5.10 常见问题排查

#### 5.10.1 小程序无法请求后端 API

**问题现象：**
- 网络请求失败
- 提示"网络请求失败"或"request:fail"

**可能原因 1：baseUrl 配置错误**
- **检查方法**：查看 `app.js` 中的 `baseUrl` 配置
- **解决方法**：
  - 确保使用本机 IP 地址，不是 `localhost`
  - 确保端口号正确（默认 8080）
  - 确保路径以 `/api` 结尾

**可能原因 2：后端服务未启动**
- **检查方法**：访问 `http://你的IP:8080/api/doctors/list`
- **解决方法**：启动后端服务（见第三章）

**可能原因 3：网络权限未配置**
- **检查方法**：查看微信开发者工具的"本地设置"
- **解决方法**：勾选"不校验合法域名"选项

**可能原因 4：防火墙阻止**
- **检查方法**：在手机上使用浏览器访问后端 API
- **解决方法**：
  - Windows 防火墙：允许 8080 端口
  - 或在防火墙中添加 Java 程序的例外

#### 5.10.2 小程序页面显示异常

**问题现象：**
- 页面空白
- 页面布局错乱
- 图片不显示

**可能原因 1：API 请求失败**
- **检查方法**：查看 Network 面板的请求状态
- **解决方法**：检查后端服务是否正常，API 地址是否正确

**可能原因 2：图片资源缺失**
- **检查方法**：查看 Console 是否有图片加载错误
- **解决方法**：确保图片资源存在于 `hms_miniprogram/assets/` 目录

**可能原因 3：页面路径配置错误**
- **检查方法**：查看 `app.json` 中的 `pages` 配置
- **解决方法**：确保页面路径正确，页面文件存在

#### 5.10.3 真机上无法访问

**问题现象：**
- 在开发者工具中正常，但真机上无法访问

**可能原因 1：网络不在同一网络**
- **检查方法**：确保手机和电脑连接同一 Wi-Fi
- **解决方法**：连接同一 Wi-Fi 网络

**可能原因 2：使用了 localhost**
- **检查方法**：查看 `app.js` 中的 `baseUrl`
- **解决方法**：使用本机 IP 地址，不要使用 `localhost`

**可能原因 3：防火墙阻止**
- **检查方法**：在手机上使用浏览器访问后端 API
- **解决方法**：配置防火墙允许 8080 端口

#### 5.10.4 编译错误

**问题现象：**
- 编译失败
- 提示语法错误或文件不存在

**可能原因 1：文件路径错误**
- **检查方法**：查看编译错误信息
- **解决方法**：确保文件路径正确，文件存在

**可能原因 2：语法错误**
- **检查方法**：查看编译错误信息
- **解决方法**：修复语法错误，检查代码格式

**可能原因 3：依赖缺失**
- **检查方法**：查看编译错误信息
- **解决方法**：确保所有依赖文件都存在

---

### 5.11 开发调试技巧

#### 5.11.1 使用 Console 调试

**查看日志输出：**
```javascript
// 在代码中使用 console.log()
console.log('调试信息：', data);
```

**查看错误信息：**
- 在 Console 面板中查看错误信息
- 错误信息会显示文件路径和行号

#### 5.11.2 使用 Network 调试

**查看网络请求：**
1. 打开 Network 面板
2. 在小程序中操作
3. 查看请求和响应数据
4. 检查请求是否成功

**分析请求问题：**
- 查看请求 URL 是否正确
- 查看请求方法（GET/POST）是否正确
- 查看请求参数是否正确
- 查看响应状态码（200 表示成功）

#### 5.11.3 使用 Storage 调试

**查看本地存储：**
1. 打开 Storage 面板
2. 查看 `localStorage` 中的数据
3. 检查就诊人列表等缓存数据

**清除本地存储：**
- 右键点击 Storage 中的数据
- 选择"清除"或"删除"

#### 5.11.4 使用断点调试

**设置断点：**
1. 在代码中点击行号，设置断点
2. 重新编译小程序
3. 触发断点处的代码
4. 查看变量值和执行流程

**单步调试：**
- 使用调试器的"单步执行"功能
- 逐步执行代码，查看变量变化

---

### 5.12 小程序发布准备（可选）

#### 5.12.1 代码审核

**检查清单：**
- ✅ 功能测试通过
- ✅ 界面显示正常
- ✅ 网络请求正常
- ✅ 无语法错误
- ✅ 无控制台错误

#### 5.12.2 配置服务器域名

**步骤 1：登录微信公众平台**
1. 访问：https://mp.weixin.qq.com/
2. 使用小程序管理员账号登录

**步骤 2：配置服务器域名**

1. 进入"开发" → "开发管理" → "开发设置"
2. 找到"服务器域名"配置
3. 添加后端服务器域名（必须使用 HTTPS）
4. 保存配置

#### 5.12.3 上传代码

**步骤 1：点击"上传"按钮**
- 在微信开发者工具顶部工具栏
- 点击"上传"按钮

**步骤 2：填写版本信息**
- 版本号：如 1.0.0
- 项目备注：描述本次更新的内容

**步骤 3：上传代码**
- 点击"上传"按钮
- 等待上传完成

#### 5.12.4 提交审核

**步骤 1：登录微信公众平台**
1. 进入"版本管理"
2. 找到刚才上传的版本
3. 点击"提交审核"

**步骤 2：填写审核信息**
- 填写小程序功能介绍
- 上传小程序截图
- 填写测试账号（如有）

**步骤 3：提交审核**
- 点击"提交审核"按钮
- 等待微信审核（通常 1-3 个工作日）

---

### 5.13 总结

完成小程序部署后，你应该能够：

1. ✅ 成功导入小程序项目
2. ✅ 配置 API 地址（使用本机 IP）
3. ✅ 配置网络请求权限
4. ✅ 成功编译和运行小程序
5. ✅ 在模拟器中测试功能
6. ✅ 在真机上预览和调试
7. ✅ 使用调试工具排查问题

**下一步：**
- 如果一切正常，可以开始测试小程序功能
- 如果遇到问题，参考本章的"常见问题排查"部分
- 如果需要发布，参考"小程序发布准备"部分

---

## 六、完整启动流程

### 6.1 启动顺序（推荐）

1. **启动 MySQL 服务**
   ```powershell
   net start MySQL
   # 或通过服务管理器启动
   ```

2. **启动后端服务**（保持 PowerShell 窗口打开）
   ```powershell
   cd D:\ProgramFiles\CodeProjects\hospital-management-system\HospitalManagementSystem_backend
   mvn spring-boot:run
   ```
   等待看到 "Started" 日志

3. **启动 Web 管理端**（打开新的 PowerShell 窗口）
   ```powershell
    cd D:\ProgramFiles\CodeProjects\hospital-management-system\web-admin
   npm run dev
   ```

4. **打开微信开发者工具**（详细步骤见第五章）
   - 导入小程序项目：选择 `hms_miniprogram` 目录
   - 配置 AppID（选择测试号）
   - 修改 `app.js` 中的 `baseUrl`（使用本机 IP 地址）
   - 配置网络请求权限（勾选"不校验合法域名"）
   - 点击"编译"按钮运行

### 6.2 快速启动脚本（可选）

你可以创建批处理文件来简化启动过程：

**创建 `start-backend.bat`**（在项目根目录）：
```batch
@echo off
cd D:\ProgramFiles\CodeProjects\hospital-management-system\HospitalManagementSystem_backend
start cmd /k "mvn spring-boot:run"
```

**创建 `start-web-admin.bat`**：
```batch
@echo off
cd D:\ProgramFiles\CodeProjects\hospital-management-system\hms_web_admin
start cmd /k "npm run dev"
```

双击这些文件即可快速启动对应服务。

---

## 七、调试方法

### 7.1 后端调试

#### 查看日志
后端启动后，控制台会显示详细日志，包括：
- API 请求日志
- SQL 执行日志
- 错误信息

#### 使用 Postman 或浏览器测试 API

**常用测试接口：**

1. **获取医生列表：**
   ```
   GET http://localhost:8080/api/doctors/list
   ```

2. **获取患者列表：**
   ```
   GET http://localhost:8080/api/patients/list
   ```

3. **获取预约列表：**
   ```
   GET http://localhost:8080/api/appointments/list
   ```

4. **医生登录：**
   ```
   POST http://localhost:8080/api/doctors/login
   Content-Type: application/json
   
   Body:
   {
     "username": "doctor1",
     "password": "123456"
   }
   ```

#### 使用 IDE 调试（如 IntelliJ IDEA）
1. 导入 `HospitalManagementSystem_backend` 项目
2. 找到主类（`HospitalManagementApplication.java`）
3. 设置断点
4. 点击"Debug"运行

### 7.2 Web管理端调试

#### 浏览器开发者工具
1. 按 `F12` 打开开发者工具
2. **Console 标签：** 查看 JavaScript 错误和日志
3. **Network 标签：** 查看 API 请求和响应
4. **Application 标签：** 查看 LocalStorage、SessionStorage 等

#### Vue DevTools 扩展
安装 Vue DevTools 浏览器扩展，可以方便地查看：
- 组件状态
- Vuex/Pinia 状态
- 组件层级

#### 修改代码实时预览
Vite 支持热更新，修改代码后会自动刷新页面。

### 7.3 小程序调试

#### 微信开发者工具调试功能

1. **Console 面板：**
   - 查看 `console.log()` 输出
   - 查看错误信息

2. **Network 面板：**
   - 查看所有网络请求
   - 检查 API 请求是否成功
   - 查看请求和响应数据

3. **Storage 面板：**
   - 查看本地存储数据
   - 检查就诊人列表等缓存数据

4. **Sources 面板：**
   - 设置断点进行调试
   - 单步执行代码

#### 真机调试
1. 点击"预览"生成二维码
2. 用微信扫码在真机上测试
3. 开启"真机调试"查看日志

### 7.4 数据库调试

#### 直接查询数据库
```sql
-- 查看所有医生
SELECT * FROM doctors;

-- 查看所有预约
SELECT * FROM appointments;

-- 查看患者信息
SELECT * FROM patients;

-- 检查预约和医生的关联
SELECT a.*, d.name as doctor_name 
FROM appointments a 
LEFT JOIN doctors d ON a.doctor_id = d.id;
```

#### 使用数据库客户端工具
推荐使用 Navicat、DBeaver、MySQL Workbench 等工具，可以：
- 可视化查看数据
- 执行 SQL 查询
- 修改数据

---

## 八、常见问题

### 8.1 数据库相关问题

**问题：无法连接数据库**
- 检查 MySQL 服务是否启动
- 检查 `application.yml` 中的数据库配置是否正确
- 检查 MySQL 用户名和密码是否正确
- 检查数据库 `hospital_management` 是否存在

**问题：表不存在**
- 确认已执行 `database/init.sql` 脚本
- 检查数据库名称是否正确

### 8.2 后端相关问题

**问题：Maven 下载依赖很慢**
- 配置国内镜像源（阿里云镜像）
- 或使用 VPN

**问题：端口 8080 被占用**
- 修改 `application.yml` 中的 `server.port` 为其他端口
- 同时修改前端和小程序的 API 地址配置

**问题：后端启动失败，提示找不到类**
- 执行 `mvn clean install` 重新编译
- 检查 JDK 版本是否为 17+

### 8.3 前端相关问题

**问题：npm install 失败**
- 清除缓存：`npm cache clean --force`
- 删除 `node_modules` 文件夹，重新执行 `npm install`
- 配置国内镜像源：`npm config set registry https://registry.npmmirror.com`

**问题：Web管理端无法访问后端 API**
- 检查后端服务是否启动
- 检查 `vite.config.js` 中的 proxy 配置
- 检查浏览器控制台的网络请求错误

**问题：页面空白或报错**
- 打开浏览器开发者工具查看错误信息
- 检查 API 请求是否成功
- 确认后端服务正常运行

### 8.4 小程序相关问题

**问题：小程序无法请求后端 API**
- 检查 `app.js` 中的 `baseUrl` 配置
- 如果是 `localhost`，改为本机 IP 地址
- 在微信开发者工具中勾选"不校验合法域名"

**问题：小程序页面显示异常**
- 检查控制台错误信息
- 确认 API 请求是否成功
- 检查页面路径配置是否正确（`app.json`）

**问题：真机上无法访问**
- 确保手机和电脑在同一网络
- 使用本机 IP 地址而非 `localhost`
- 检查防火墙是否阻止了 8080 端口

### 8.5 综合问题

**问题：所有服务都启动但无法通信**
- 检查防火墙设置，确保 8080 和 5173 端口未被阻止
- 检查各个服务的地址配置是否正确
- 查看各服务的控制台日志，寻找错误信息

**问题：如何查看详细的错误日志**
- 后端：查看启动时的控制台输出
- Web前端：打开浏览器开发者工具的 Console 和 Network 面板
- 小程序：在微信开发者工具的 Console 和 Network 面板查看

---

## 九、项目文件说明

### 9.1 重要配置文件位置

| 文件 | 位置 | 说明 |
|------|------|------|
| 数据库配置 | `HospitalManagementSystem_backend/src/main/resources/application.yml` | 修改数据库连接信息 |
| 后端端口 | `HospitalManagementSystem_backend/src/main/resources/application.yml` | 默认 8080 |
| Web API 代理 | `hms_web_admin/vite.config.js` | 代理后端 API |
| 小程序 API 地址 | `hms_miniprogram/app.js` | 后端服务地址 |
| 数据库脚本 | `hms_database/init.sql` | 初始化数据库 |

### 9.2 测试账号

**医生端（Web管理端）：**

- 用户名：`doctor1`, `doctor2`, ..., `doctor10`
- 密码：`123456`

**患者端（小程序）：**
- 通过小程序添加就诊人后使用

---

## 十、开发建议

### 10.1 开发流程

1. **先启动后端服务**，确保 API 正常
2. **再启动前端服务**，进行界面开发
3. **使用浏览器开发者工具**调试前端
4. **使用 Postman** 测试后端 API
5. **最后在小程序中测试**完整流程

### 10.2 代码修改建议

- 修改后端代码后，需要重启后端服务
- 修改前端代码后，Vite 会自动热更新
- 修改小程序代码后，需要重新编译

### 10.3 版本控制

建议使用 Git 进行版本控制：
```powershell
cd D:\ProgramFiles\CodeProjects\hospital-management-system
git init
git add .
git commit -m "初始提交"
```

---

## 十一、云端部署与赛事准备

参赛或上线时，建议将服务迁移到云环境，避免依赖个人热点或局域网。

### 11.1 华为云部署思路概览

1. **数据库（RDS MySQL）**
   - 在华为云创建 RDS 实例，初始化 `hospital_management` 数据库，执行 `hms_database/init.sql`。
   - 在后端 `application.yml` 中配置 RDS 内/公网地址、账号密码。
2. **后端服务（华为云 ECS）**
   - 安装 JDK 17 与 Maven。
   - 从代码仓库拉取项目，`mvn clean package`，使用 `java -jar` 启动。
   - 云服务器安全组开放 80/443/8080 端口，确保内部能访问 RDS。
3. **反向代理与 HTTPS**
   - 安装 Nginx，将外部域名（如 `https://hms.example.com`）反向代理到本地 8080。
   - 申请并配置 SSL 证书（可使用华为云免费证书），实现全站 https。
   - 在 Nginx 中将 `/api/` 与 `/files/` 统一转发给后端。
4. **附件存储选项**
   - 继续使用服务器磁盘存储（需做好数据备份）。
   - 或接入华为云 OBS，将 `fileUrl` 替换为 OBS 外链 URL，前端逻辑无需改动。
5. **小程序域名配置**
   - `baseUrl` 改为 `https://hms.example.com/api`。
   - 微信公众平台 → 服务器域名 → 添加 `https://hms.example.com` 为 request/uploadFile 合法域名。

### 11.2 自动化与运维建议

- 使用 Git 管理版本，结合华为云 DevCloud 或 GitHub Actions 持续部署。
- 配置日志采集与监控，及时发现数据库连接或网络异常。
- 为数据库、上传文件做定期备份，确保数据安全。

> 采用云部署后，真机、小程序审核、Web 端均可直接访问公网服务，避免网络穿透与热点的额外操作，更符合比赛与生产要求。

---

## 总结

按照本指南的步骤，应该能够完成：
1. ✅ 成功部署数据库
2. ✅ 成功启动后端服务
3. ✅ 成功启动 Web 管理端
4. ✅ 成功运行微信小程序
5. ✅ 进行基本的调试和测试

如果遇到问题，请参考"常见问题"部分，或查看各服务的日志输出。

**祝开发顺利！** 🎉

