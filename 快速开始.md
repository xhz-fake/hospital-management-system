# 快速开始 - 5分钟部署指南

## 🚀 超快速部署步骤

### 第一步：检查环境
双击运行 `环境检查.ps1`，确保所有软件已安装。

### 第二步：初始化数据库
```powershell
# 1. 登录 MySQL
mysql -u root -p

# 2. 创建数据库（在 MySQL 中执行）
CREATE DATABASE IF NOT EXISTS hospital_management CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE hospital_management;

# 3. 退出 MySQL，执行初始化脚本
mysql -u root -p hospital_management < hms_database\init.sql
```

### 第三步：配置数据库连接
编辑文件：`HospitalManagementSystem_backend\src\main\resources\application.yml`

修改：
```yaml
username: "root"      # 改为你的 MySQL 用户名（建议加双引号）
password: "<你的MySQL密码>"       # 改为你的 MySQL 密码
```

### 第四步：启动后端（方式一：双击批处理文件）
双击 `启动后端.bat`

或使用命令行：
```powershell
cd HospitalManagementSystem_backend
mvn spring-boot:run
```

等待看到：`Started HospitalManagementApplication`

### 第五步：启动Web管理端

**⚠️ 重要：确保后端服务已启动（见第四步）**

**方式一：双击批处理文件**
双击 `启动Web管理端.bat`

**方式二：使用命令行**
```powershell
cd web-admin
npm install  # 仅首次运行需要
npm run dev
```

**等待看到：**
```
VITE v5.x.x  ready in xxx ms
➜  Local:   http://localhost:5173/
```

**然后在浏览器访问：** http://localhost:5173

### 第六步：打开微信开发者工具
1. 导入项目：选择 `hms_miniprogram` 目录
2. 配置 AppID（选择测试号）
3. 修改 `hms_miniprogram/app.js` 中的 `baseUrl`（如果需要）
4. 点击"编译"运行

---

## 📍 访问地址

| 服务 | 地址 | 说明 |
|------|------|------|
| 后端API | http://localhost:8080/api | 测试：http://localhost:8080/api/doctors/list |
| Web管理端 | http://localhost:5173 | 账号：doctor1，密码：123456 |
| 小程序 | 微信开发者工具 | 需要配置 AppID |

---

## 🔑 测试账号

**Web管理端（医生端）：**
- 用户名：`doctor1` 到 `doctor10`
- 密码：`123456`

---

## ⚠️ 常见问题速查

| 问题 | 解决方法 |
|------|---------|
| 数据库连接失败 | 检查 `application.yml` 中的数据库配置 |
| 端口被占用 | 修改 `application.yml` 中的端口号 |
| npm install 失败 | 执行 `npm cache clean --force` 后重试 |
| 小程序无法请求 | 将 `localhost` 改为本机 IP 地址 |
| Maven 下载慢 | 配置国内镜像源 |
| IDE控制台中文乱码 | 见部署指南 3.5.8 节详细说明 |

---

## 📚 详细文档

完整部署指南请查看：**部署指南.md**（位于项目根目录）

---

## 🎯 验证部署成功

1. ✅ 访问 http://localhost:8080/api/doctors/list 能看到 JSON 数据
2. ✅ 访问 http://localhost:5173 能看到登录页面
3. ✅ 能成功登录 Web 管理端
4. ✅ 小程序能正常显示界面

**全部完成 = 部署成功！** 🎉

